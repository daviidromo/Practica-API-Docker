import 'bootstrap/dist/css/bootstrap.min.css'
import 'bootstrap'

import { createApp } from 'vue'
import App from './App.vue'

// Hemos quitado la línea del "router" para que no de error
createApp(App).mount('#app')